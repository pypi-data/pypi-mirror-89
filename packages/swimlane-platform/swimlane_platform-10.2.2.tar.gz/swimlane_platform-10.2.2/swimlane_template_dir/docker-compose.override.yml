# Use this file to override or extend configuration from the default Swimlane
# docker-compose.yml file

version: '3.7'

services:
  sw_web:

  sw_mongo:

  sw_api:
    #volumes:
    #  - /path/to/thirdpartycerts:/usr/local/share/ca-certificates/swimlane/
  sw_tasks:
    #volumes:
    #  - /path/to/local/workspace:/app/workspace
    #  - /path/to/thirdpartycerts:/usr/local/share/ca-certificates/swimlane/
  #sw_syslog_receiver:
  #  container_name: sw_syslog_receiver
  #  image: <sw_docker_image_repo_placeholder>swimlane/swimlane-syslog-receiver:latest
  #  restart: unless-stopped
  #  volumes:
  #    - syslog-vol:/app/log/syslog-receiver
  #  ports:
  #    - 514:5514/udp
  #chrome:
  #  container_name: chrome
  #  image: selenium/standalone-chrome:3.141.59
  #  restart: unless-stopped
  #  shm_size: '2gb'
  #  networks:
  #    - internal_network
#volumes:
#  syslog-vol:
#    external: false
