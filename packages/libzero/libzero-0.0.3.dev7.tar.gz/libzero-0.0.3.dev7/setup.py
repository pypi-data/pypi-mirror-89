#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['zero', 'zero.tests']

package_data = \
{'': ['*']}

install_requires = \
['numpy >=1.17,<2', 'pynvml >= 8.0,<9', 'torch >= 1.4,<2', 'tqdm >= 4.0,<5']

setup(name='libzero',
      version='0.0.3.dev7',
      description='A zero-overhead library for PyTorch users.',
      author='Yura52',
      author_email='strausmg@gmail.com',
      url='https://github.com/Yura52/zero',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      python_requires='>=3.6',
     )
