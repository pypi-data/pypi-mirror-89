from setuptools import setup

setup(name='distributions_wdp',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      author='Welly',
      zip_safe=False)
