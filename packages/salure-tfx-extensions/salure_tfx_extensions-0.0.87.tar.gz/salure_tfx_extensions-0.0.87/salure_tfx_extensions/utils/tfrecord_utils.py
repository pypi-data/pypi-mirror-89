# import numpy as np
# import tensorflow as tf
#
#
# def pandas_to_tfrecords(df, folder, compression_type='GZIP', compression_level=9, columns=None):
#     schema = _get_schema(df, columns)
#
#
# def _get_schema(df):
#     schema = {}
#
#     for column_name, dtype in df.dtypes.items():
