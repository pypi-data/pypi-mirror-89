from setuptools import setup

setup(name='first_gauss_binomial_package',
      version='0.1',
      description='Gaussian distributions',
      packages=['first_gauss_binomial_package'],
      zip_safe=False)
