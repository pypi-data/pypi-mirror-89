from setuptools import setup

setup(name='py_distributions_1',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['py_distributions_1'],
      zip_safe=False)
