from setuptools import setup

setup(name='cnemri_distributions',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['cnemri_distributions'],
      zip_safe=False)
